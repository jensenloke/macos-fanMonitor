"""Opt-in AI diagnosis layer for `fm`.

Everything here is additive: the deterministic verdict / recommend path never
touches it. The agent only *recommends* closes — it can never kill anything
itself.
"""
